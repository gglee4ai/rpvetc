% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RG199R2.R
\name{rg199r2_sd}
\alias{rg199r2_sd}
\title{Sigma Delta (SD) Calculation from Surveillance Test Data}
\usage{
rg199r2_sd(product_form, SV_flu = NULL, SV_tts = NULL)
}
\arguments{
\item{product_form}{character vector, specifying the product form. Must be one of \code{"B"} (base metal) or \code{"W"} (weld metal).}

\item{SV_flu}{numeric vector, neutron fluence in n/cm² from surveillance test data. Optional if SD is determined solely by \code{product_form}.}

\item{SV_tts}{numeric vector, temperature transition shift (TTS) in degrees Fahrenheit (°F) from surveillance test data. Optional if SD is determined solely by \code{product_form}.}
}
\value{
A numeric vector representing the sigma delta (SD) in degrees Fahrenheit (°F).
}
\description{
Computes the sigma delta (SD) based on surveillance test data and product form using Regulatory Guide 1.99 Rev. 2 guidelines.
The SD is determined either from the product form alone or using surveillance fluence and TTS data.
}
\examples{
# Example 1: Calculate SD based only on product form
rg199r2_sd(c("B", "W"))

# Example 2: Calculate SD using surveillance test data
rg199r2_sd(c("B", "B"), c(1e18, 5e19), c(10, 50))

}
